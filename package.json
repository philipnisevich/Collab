{
  "name": "collab",
  "version": "0.1.0",
  "description": "Kylon-coordinated Claude Code — two devs, one repo, zero merge conflicts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'",
    "install-hooks": "node hooks/install.mjs"
  }
}
